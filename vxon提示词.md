# Vxon 官网设计提示词 — 完整版

---

## 项目背景


**我们是谁：** Vxon 是一家专注于企业级AI服务的科技公司，核心团队拥有多年大模型应用和系统集成经验。我们不做噱头，做的是企业真正能用起来的AI基础设施。

**我们的产品矩阵：**

- **VxonAPI** — 大模型聚合平台。个人和企业都能用，一站式接入 Google Gemini、OpenAI GPT、Anthropic Claude、DeepSeek、Qwen、GLM、Kimi 等主流大模型。支持灵活切换模型、余额实时查询、对标官网7折起售。适合有批量AI调用需求的团队。

- **VxonAI** — 企业级AI私有化部署方案。针对安全管控要求高的企业，支持本地私有化部署大模型，所有数据不出内网。统一账户管理体系无缝集成企业现有系统，专门针对办公协同和研发编码场景优化，帮助企业提效。

- **VxonClaw** — 企业智能体平台。深度融合运营、财务、销售、经管等业务部门，可以替代传统的 BI 系统、运营系统、CRM 系统，同时与 OA 办公系统融合，实时为业务赋能。不是简单的工具堆砌，是真正嵌入业务流程的智能化管控平台。

- **VxonFort ZT** — 零信任AI编码安全堡垒机（筹备中）。将 Claude Code 封装进零信任管控体系，支持网络隔离、研发区隔离、出站API审计过滤。代码不可直接拷贝，任何 Copy 操作自动触发审批流，审批通过后方可释放，一键部署启动，满足企业等保合规要求。这个产品可以单独做一个「即将上线」的预告页面吊胃口。

**我们的客户画像：** 企业决策者（CTO、CIO）、技术负责人、研发团队、以及有大规模AI调用需求的个人开发者。他们不是AI小白，他们关心的是稳定性、安全性、价格和服务保障。

**我们想传达的感觉：** 专业、靠谱、技术实力强、有未来感。不土、不俗、不套版。

---

## 一、整体设计语言

### 整体气质

深色主基调，大量留白，克制而精准。页面里的每个元素都有它的理由，不堆砌，不废话。像 Linear + Stripe + Anthropic 那种调性——冷静、克制、精确、科技感从骨子里透出来，不是贴上去的。

### 空间节奏

页面节奏是：**紧—松—紧—松—紧—松**。

- 英雄区：大留白，信息密度低，让用户聚焦
- 产品卡片区：中等密度，内容紧凑但有序
- 优势区：宽松，三四列布局呼吸感强
- 场景区：中等，图文穿插节奏感
- CTA区：极简，一句话加两个按钮
- Footer：信息密度回升，实用为主

整体模块之间用 80-120px 的间距分隔。模块内部用 24-48px 的间距。段落行高 1.6-1.8，不要太挤。

### 动效哲学

动效是骨骼的呼吸，不是皮肤的花纹。所有动效遵循以下原则：

- **入场动画**：页面加载时，元素从下方 fade+translate 渐入，交错出现（每个元素延迟 80-100ms），不是一起蹦出来
- **视差滚动**：模块之间有轻微的进入速度差，形成层次感
- **Hover 反馈**：按钮上浮+光晕，边框渐亮，文字颜色过渡，200ms ease-out
- **背景动效**：粒子/网格/数据流，透明度 10-15%，速度极慢（15-25秒循环），用户不会注意到它在动，但能感觉到页面是活的
- **禁止**：弹跳动画、闪烁、3D翻转、进度条式加载

### 配色基调

黑底白字蓝点缀。强调色只用来做三件事：按钮主色、hover 高亮、关键数字突出。其他地方一律用灰阶。

---

### 科技感专项描述

这一节是科技感的核心实现指南，每个模块都有对应的视觉和动效要求，严格执行。

#### 全站背景层

整个网站在内容层之下，始终存在一层看不见但能感知的背景层。这层不抢内容，但让页面充满「智能在运转」的氛围：

- **全屏暗色网格**：页面最底层是一张极淡的网格线，颜色 `rgba(强调色, 0.03)`，线条间距 48-64px，线条宽度 1px。这张网格在页面加载完成后，从上往下缓慢滚动（模拟深度感），30秒完成一个完整循环，速度慢到用户几乎感知不到，但整个页面看起来是「活的」，不是静止的死页面
- **全局粒子场（可选）**：在整个页面上层叠加一层稀疏的粒子效果，粒子极小（1-2px），颜色 `rgba(强调色, 0.1)`，数量极少（每屏不超过 20 个），每个粒子缓慢随机漂浮，速度极慢，有粒子出现也有粒子消失，透明度在 0-0.15 之间波动。这个效果在所有页面都存在，但透明度足够低，不影响阅读

#### Hero 首屏 — 科技感主战场

Hero 是科技感的门面，这里必须做得足够有冲击力，但不是花哨，而是精准的克制。

**三层背景叠加（从下到上）：**

```
第一层（最底层）：深色背景 + 全屏暗色网格 + 网格向下缓慢滚动
第二层（中层）：抽象动效层（Canvas/WebGL，透明度 12-18%）
第三层（最上层）：内容层
```

**第二层动效 — 三选一，推荐方案A：**

- **方案A（推荐）：神经网络数据流 — 聚合感**

  全屏 Canvas，背景纯黑偏深蓝（#080c14）。画面中央偏下位置绘制一个抽象的神经网络：数十个节点散布，每个节点是一个 2-4px 的发光点，颜色强调色或纯白。每个节点与其他节点之间有细线连接（透明度 0.05-0.15），模拟神经网络结构。

  动效细节：
  - 节点有呼吸动画：每个节点的透明度在 0.3-1 之间缓慢波动，周期 3-8 秒随机，各节点周期不同步，形成自然的闪烁感
  - 数据在连线上流动：每隔 1-2 秒，有一个小光点（1-2px）沿着某条连线从 A 节点流向 B 节点，流速缓慢，光点有尾迹（渐隐效果）
  - 连线有呼吸：连线的透明度随时间缓慢变化，模拟「信号在传递」
  - 整体不动，只是微微呼吸。不要有大幅移动、旋转、缩放

- **方案B：六边形蜂巢呼吸 — 结构感**

  全屏 Canvas，绘制一个六边形蜂巢网格，覆盖 Hero 区域但不超出。六边形边框颜色 `rgba(强调色, 0.06)`，填充透明。

  动效细节：
  - 有一个「光波」从画面中心向外扩散，速度极慢（每 8-12 秒一个波）
  - 光波经过的六边形，边框颜色短暂变亮到 `rgba(强调色, 0.2)`，然后渐隐
  - 每个六边形的边框透明度有细微差异，整体看起来像在「呼吸」
  - 偶尔有一两个六边形的填充出现 `rgba(强调色, 0.03)` 的微光，像心跳

- **方案C：代码数据流雨 — 技术感**

  参考 Matrix 数字雨，但不是那种花花绿绿的风格。整屏极淡的代码/数字从上方流入，字体极小（10-12px），颜色 `rgba(强调色, 0.04)`，从右上方斜向左下方流动，速度极慢（比 Matrix 慢 5-8 倍），每条流之间间距极大（至少 40-60px），整体感觉是「数据在暗处流淌」，不是抢眼的视觉。

**Hero 文字光效：**

- 大标题文字可以有极其微妙的光泽感：`text-shadow: 0 0 60px rgba(强调色, 0.12)`
- 如果标题是英文，可以在部分单词上使用微妙的渐变文字效果（color 渐变从白到强调色再回到白），透明度极低，像是文字内部在发光

**Hero 文字入场后的持续动效：**

- 大标题在首次入场后，保持静态，但每隔 20-30 秒，标题的光晕有极其微妙的亮度波动（从 0.12 到 0.18 再回到 0.12），模拟「生命感」，这个效果极其微妙，用户几乎感知不到，但会觉得标题是「活」的

#### 导航栏动效

- 导航栏背景从透明变为毛玻璃时，过渡时长 300ms，带有 blur 从 0 到 20px 的同步动画
- 下拉面板展开时：高度从 0 展开，opacity 从 0 到 1，时长 200ms，ease-out。收起时反向，150ms
- 导航栏右侧 CTA 按钮「免费试用」在鼠标悬停时：背景从纯色变成带微弱光晕的渐变，同时按钮周围出现一个 1px 的强调色外边框，box-shadow：`0 0 20px rgba(强调色, 0.2)`，上浮 2px

#### 产品卡片区 — 科技感细节

**每个产品卡片的科技感处理：**

- 卡片顶部有一个极其微妙的渐变条（2px 高，从左到右，从强调色渐变到透明），初始透明度 0，hover 时透明度变为 0.5，这个细节暗示「这是有活力的」
- 产品图标本身有持续的发光呼吸：图标周围有一个极淡的光晕（box-shadow 8px），光晕的透明度在 0.05-0.15 之间缓慢波动，周期 4-6 秒，不同产品的图标周期不同步

**VxonFort ZT 卡片（敬请期待）的特殊处理：**

- 卡片边框有微妙的闪烁效果：边框颜色在 `rgba(强调色, 0.1)` 到 `rgba(强调色, 0.25)` 之间缓慢变化，周期 3-4 秒，像是「心跳」一样，暗示产品即将到来
- 卡片左下角有一个小的脉冲圆点（6px），颜色强调色，每 2 秒做一次 opacity 0.3→1→0.3 的脉冲动画

#### 核心优势区 — 数据感

**每个优势的数字：**

- 数字本身在视觉上要有「悬浮感」：数字区域上方 8px 处有一个微妙的水平反光条（1px 高，渐变透明），像是金属光泽
- 数字入场时有打字机效果：从 0 开始滚动到目标数字，时长 1500ms，使用 easeOut 曲线，只触发一次

#### 合作厂商展示区 — 科技感处理

- 每个 Logo 在页面加载完成后，有一个微妙的「点亮」效果：从灰暗（opacity 0.4）逐渐变亮到正常（opacity 0.7），每个 Logo 依次出现，delay 50ms，模拟「系统启动、厂商逐一上线」的感觉
- Hover 时 Logo 恢复正常颜色（opacity 1）+ 微微发光（box-shadow 0 0 8px）

#### 数据与证明区 — 数字仪表感

- 数字入场后，持续有一个微妙的「仪表盘」效果：数字下方有一个极短的进度条（宽度为数字宽度的 1/4），进度条的颜色是强调色，透明度 0.3，这个进度条持续存在，模拟「仪表在运转」
- 数字本身每隔 10-15 秒有极其微妙的亮度波动（opacity 从 1 到 0.95 再回到 1）

#### VxonAPI 产品页 — API 感

**专属背景动效：**

在主站 Hero 动效基础上，加入 API 相关的视觉暗示。画面中央有 5-8 条细线从不同方向（上下左右斜向）向中心汇聚，汇聚点是一个 4-6px 的发光核心。这 5-8 条线代表不同的模型/数据源汇聚到 VxonAPI 这一个平台。

每条线的透明度在 0.05-0.2 之间随机缓慢变化，各线独立，有光点沿着线向中心缓慢移动，移动速度极慢，象征「数据汇聚」。

**快速开始区的代码块：**

代码块内部有持续的「打字机」效果（极其微妙）：代码块内有一行代码（或者注释），每隔 8-10 秒，微妙的透明度在 0.3-0.6 之间变化，像是有人在「实时查看」这个接口的响应。

#### VxonAI 产品页 — 安全隔离感

**专属背景动效：**

画面中央有一个核心节点（6-8px 发光点），周围有 3 层同心圆环，每层圆环颜色不同（由内到外：强调色 → `rgba(强调色, 0.3)` → `rgba(强调色, 0.1)`），圆环透明度 0.2-0.4。

动效细节：
- 圆环有缓慢的呼吸效果：半径在 ±3% 之间缓慢变化，周期 6-8 秒，三层圆环周期错开，模拟「心跳」
- 核心节点持续发光，光晕有微妙的脉动
- 偶尔有一两个数据点从最外层消失，然后从最内层出现（象征「数据进入隔离区后完全内化」），但这个效果极慢，1-2 分钟才发生一次

#### VxonClaw 产品页 — 流程连接感

**专属背景动效：**

画面中有 5-6 个节点散布（类似神经网络），节点之间有连线，但不是全连接，而是形成一种「星形+链形」的混合结构，象征不同业务部门连接到统一平台。

动效细节：
- 每个节点是一个小光点，持续缓慢闪烁（透明度 0.4-1，周期 2-5 秒随机）
- 连线上偶尔有光点从一端流到另一端（数据在部门之间流动），但数量极少，速度极慢，大部分时候连线是静态的半透明细线
- 部门覆盖区的放射状图形，线条有缓慢的发光脉冲，模拟「平台在持续连接所有部门」

#### VxonFort ZT 产品页 — 零信任管控感

**专属背景动效：**

这是科技感最强的页面，要做得像「安全管控中心」。

画面中央是一个被严格「包裹」的核心：
- 最内核：6-8px 的白色发光核心
- 第二层：一个不规则多边形（类似多边形的盾牌轮廓），边框颜色强调色，透明度 0.4，边框有持续的微弱闪烁
- 第三层：一个更大的圆形轮廓，比第二层大半径 1.5 倍，透明度 0.2，缓慢旋转（速度极慢，60-90 秒完成一圈）
- 最外层：4-6 条细线从外向内延伸，但每条线在距离核心 30% 的位置就被「拦截」了，线端点有一个小圆点，表示「流量被审计」

动效细节：
- 核心有稳定的强光（box-shadow 大范围光晕），模拟「被保护的核心」
- 所有层都在缓慢呼吸（透明度 ±10%），但呼吸不同步，层次感强
- 偶尔有一个小数据点从外层「进入」，但在第二层被拦截（停顿 1-2 秒），然后「消失」（被吸收），模拟「API 审计流程」

#### 所有页面通用 — 科技感微细节

- **扫描线效果（可选）**：页面最顶层叠加一层极淡的横向扫描线，扫描线每 8-12 秒从顶部移动到底部，速度慢，透明度 0.02-0.04，像 CRT 显示器的感觉。不影响阅读，但增加「技术设备感」
- **故障艺术（Glitch）效果（极低频使用）**：只在关键标题上偶尔出现，比如 Hero 大标题每隔 30-60 秒，可能出现一次极其微妙的 Glitch 效果：文字微微偏移 1px，颜色微微分离（RGB 分离 1px），持续 50-100ms 就恢复。这个效果要有，但不能频繁
- **光标跟随效果（可选）**：页面背景层有一个极淡的光晕（8-12px），跟随鼠标移动，但透明度极低（0.03），移动速度有轻微延迟（easing），像是在「感知」用户的操作

#### 科技感的底线

以上所有动效都要遵守以下原则，违反任何一条都要删除：

1. **透明度上限**：任何动效元素的透明度不得超过 0.25，超过就抢眼
2. **速度下限**：任何动效的周期不得低于 1.5 秒（即完成一次完整动效至少需要 1.5 秒），低于这个速度会让人眼花
3. **静止优先**：如果动效影响阅读或者让人分心，宁可删掉。科技感是氛围，不是表演
4. **背景层原则**：所有动效元素（除按钮反馈外）必须在内容层之下，不能遮挡文字和按钮
5. **性能原则**：所有动效必须在 60fps 下流畅运行，Canvas/WebGL 动效要注意性能优化

---

---

## 二、主站（Homepage）

### 2.1 导航栏 Nav

**整体描述：**

固定顶部，高度 64px。初始透明，滚动超过 80px 后背景变成毛玻璃效果（`backdrop-filter: blur(20px)`）。Logo 在最左侧，主菜单居中，CTA 按钮组在右侧。

**Logo 区域：**

左侧放 Vxon 品牌 Logo，SVG 格式，白色。如果有图标版本，图标居左，文字居右对齐。

**主菜单（5项横向排列）：**

1. **产品** — 悬停展开产品下拉面板（Mega Menu）
2. **解决方案** — 悬停展开场景下拉面板
3. **价格** — 点击跳转 `/pricing`
4. **文档** — 点击新窗口打开 `/docs`
5. **关于我们** — 悬停展开公司信息下拉

**菜单 Hover 效果：**

文字颜色从灰色过渡到白色，200ms。在文字下方出现一条细横线，从中间向两边展开，200ms。

**下拉面板 — 产品 Mega Menu（鼠标悬停「产品」展开）：**

面板从导航栏下方滑出，背景毛玻璃效果，边框，圆角。

面板内部三列布局：

- 第一列：VxonAPI 的小卡片（图标+名称+一句话简介+入口链接）
- 第二列：VxonAI 的小卡片
- 第三列：VxonClaw 小卡片（左侧），VxonFort ZT 小卡片（右侧，下方加「敬请期待」标签）

每个产品小卡片内：
- 左上角产品图标
- 下方产品中文名（粗体）
- 下方产品英文名（灰色）
- 下方一句话描述（灰色）
- 底部「进入 →」链接

小卡片 hover 时整卡背景变亮，左边框出现细竖线。

**下拉面板 — 解决方案下拉：**

三列，每列一个场景入口卡片：
- 企业AI私有化部署（对应 VxonAI）
- 智能体业务管控（对应 VxonClaw）
- 编码安全合规（对应 VxonFort ZT）

**下拉面板 — 关于我们下拉：**

两列：
- 左侧：公司介绍 / 加入我们 / 联系方式（竖排文字链接）
- 右侧：一张抽象的团队/技术插图（灰色调，不抢眼）

**CTA 按钮组（右侧）：**

两个按钮横向排列：
- 「登录」（次按钮，文字链接，灰色，hover 变白）
- 「免费试用」（主按钮，填充强调色，hover 上浮+发光）

**移动端响应：**

断点 768px。右侧按钮保留「免费试用」，左侧改为汉堡图标。点击汉堡图标从右侧滑入全屏菜单面板。菜单项纵向排列，文字较大。最底部放「登录」链接。

---

### 2.2 Hero 首屏

**空间布局：**

Hero 区域占满一屏（100vh），内容垂直居中水平居中。内容区宽度最大 960px。

**文字层级（纵向排列，从上到下）：**

1. **顶部标签小字**（延迟0ms）— 如「Vxon — 企业级AI基础设施」
2. **大标题**（延迟100ms）— 建议「企业级AI基础设施，稳定、安全、高性价比」或「让每一家企业都能用上大模型」
3. **副标题**（延迟200ms）— 两行以内，概括核心价值
4. **CTA按钮组**（延迟300ms）— 两个按钮横向排列
5. **底部滚动引导箭头**（脉冲动画）— 居中，距离底部一定距离

**大标题：**

建议用「企业级AI基础设施，稳定、安全、高性价比」。字号大，粗体，行高紧凑。可以有微妙的光泽效果。

**副标题：**

两行以内，中等字号，灰色。内容说清楚 VxonAPI 是什么、接了哪些模型、有什么价格优势，VxonAI 是什么、解决什么问题。80字以内，读完不累。

**CTA 按钮组：**

- 「立即体验」— 主按钮：填充强调色渐变，白色字。Hover：上浮，box-shadow 外发光加大
- 「了解产品」— 次按钮：透明背景，白色边框，白色字。Hover：背景变亮，边框变强调色

两个按钮横向排列，间距适中。

**背景动效区域（覆盖整个 Hero）：**

背景基础层：纯深色。

第一层：全屏网格背景，线条颜色极淡，缓慢向下滚动模拟深度感（30秒完成一个完整循环）。

第二层：在 Hero 区域中心偏下的位置，放一个抽象的数据流动画。必须在以下三个方案中选择一个，禁止混用：

- **方案A（推荐）：神经网络数据流 — 聚合感**

  全屏 Canvas，背景纯黑偏深蓝。画面中央偏下位置绘制一个抽象的神经网络：数十个节点散布，每个节点是 2-4px 的发光点，颜色用强调色或纯白。每个节点与其他节点之间有细线连接（透明度 0.05-0.15），模拟神经网络结构。

  动效细节：
  - 节点有呼吸动画：每个节点的透明度在 0.3-1 之间缓慢波动，周期 3-8 秒随机，各节点周期不同步，形成自然的闪烁感
  - 数据在连线上流动：每隔 1-2 秒，有一个小光点（1-2px）沿着某条连线从 A 节点流向 B 节点，流速缓慢，光点有尾迹（渐隐效果）
  - 连线有呼吸：连线的透明度随时间缓慢变化，模拟「信号在传递」
  - 整体不动，只是微微呼吸。不要有大幅移动、旋转、缩放

- **方案B：六边形蜂巢呼吸 — 结构感**

  全屏 Canvas，绘制一个六边形蜂巢网格，覆盖 Hero 区域但不超出。六边形边框颜色极淡强调色，填充透明。

  动效细节：
  - 有一个「光波」从画面中心向外扩散，速度极慢（每 8-12 秒一个波）
  - 光波经过的六边形，边框颜色短暂变亮，然后渐隐
  - 每个六边形的边框透明度有细微差异，整体看起来像在「呼吸」
  - 偶尔有一两个六边形的填充出现微光，像心跳

- **方案C：代码数据流雨 — 技术感**

  参考 Matrix 数字雨，但不是花花绿绿的风格。整屏极淡的代码/数字从上方流入，字体极小，颜色极淡强调色，从右上方斜向左下方流动，速度极慢（比 Matrix 慢 5-8 倍），每条流之间间距极大（至少 40-60px），整体感觉是「数据在暗处流淌」，不是抢眼的视觉。

动效整体透明度控制在 10-15%，色调用强调色或纯白。速度极慢，用户不应该明显感知到它在动，但整个 Hero 区域看起来是「活的」。

**滚动引导箭头：**

Hero 最底部居中，放一个向下的小箭头。初始半透明，每几秒做一次脉冲动画。点击后平滑滚动到下一个模块。

---

### 2.3 产品家族展示区

**空间布局：**

上下大padding，左右居中。区块标题居左。

**区块结构：**

- 区块顶部：左侧文字区
- 四个产品卡片等宽横排，间距适中

**区块标题区：**

顶部一行小字标签「产品家族」或「Product Family」。

下方大标题，白色。

下方一行描述文字，灰色。

区块顶部到卡片之间留足间距。

**四个产品卡片（横排，PC端一行4个）：**

每个卡片圆角，背景半透明，边框。

卡片内部结构（从上到下）：

```
[产品图标区：圆角背景，居左]
  ↓
[产品中文名 + 产品英文名]
  ↓
[一句话描述：灰色，最多2行]
  ↓
[功能Tag区：多行Tag横向排列]
  ↓
[底部链接：「进入产品 →」]
```

**VxonAPI 卡片内容：**

图标：API 接口相关的图标
描述：一站式接入 Google Gemini、OpenAI GPT、Claude、DeepSeek 等主流大模型，对标官网 7 折起
Tags：ChatGPT / Claude / Gemini / DeepSeek / Qwen / 7折起 / API即用 / 余额实时

**VxonAI 卡片内容：**

图标：服务器或锁的图标
描述：本地私有化部署大模型，内网安全运行，统一账户管理，深度融合企业办公与研发场景
Tags：私有化部署 / 内网安全 / 统一账户 / 7×24H / 企业级

**VxonClaw 卡片内容：**

图标：爪子或者齿轮+AI的组合图标
描述：融合运营、财务、销售、经管业务，替代 BI/CRM/OA，实时为业务赋能，降本增效
Tags：智能BI / CRM融合 / OA联动 / 业务赋能 / 降本增效

**VxonFort ZT 卡片内容：**

图标：盾牌或锁的图标
描述：将 Claude Code 封装进零信任管控，网络隔离 + API审计 + Copy审批流一键部署
Tags：🔒 敬请期待 / 零信任 / 等保合规 / 审批流

**卡片 hover 效果：**

- 整体上浮
- 背景变亮
- 边框出现淡淡的光晕
- 产品图标微微发光
- 底部链接文字向右位移

**卡片入场动画：**

页面滚动到该区域时，四个卡片从下往上交错入场。第一个卡 delay 0ms，第二个 delay 80ms，第三个 delay 160ms，第四个 delay 240ms。

---

### 2.4 核心优势区

**空间布局：**

上下大padding。背景可以与上层略有区分（比如加一个微妙的渐变）。

**区块标题：**

上方一行小字标签，下方大标题。

**优势点布局：**

2×2 网格，每个格子等宽，间距适中。

每个优势格子的结构：

```
[上方图标区：线性图标，强调色，居左]
[上方数字强调：「7折」「24H」「4层」等，大字号，白色]
[优势标题：粗体，白色]
[优势描述：灰色，行高宽松，最多3行]
```

**四个优势内容：**

**性能稳定**
图标：闪电图标
数字：「99.9%」或「7×24H」
标题：性能稳定 · 量大管饱
描述：7×24 小时稳定服务保障，高并发承载能力，接口响应快，不掉线不抽风。企业级 SLA 保障，放心用。

**价格优势**
图标：钱包或货币图标
数字：「7折」
标题：价格优势 · 对标官网7折起
描述：聚合 Google、OpenAI、Anthropic、DeepSeek 等主流模型，绕过中间商，量大从优，用多少付多少，没有套路。

**安全合规**
图标：盾牌或锁图标
数字：「4层」
标题：安全合规 · 企业级数据保障
描述：数据权限精细化隔离，统一账户管控，支持私有化部署，满足金融、政务等高安全合规要求。

**灵活接入**
图标：插头或连接图标
数字：「5分钟」
标题：灵活接入 · 即开即用
描述：兼容 OpenAI 标准 API 格式，现有代码无需改动，5 分钟完成接入和切换，零学习成本。

**入场动画：**

2×2 网格，左上→右上→左下→右下的顺序交错入场。

---

### 2.5 应用场景区

**空间布局：**

上下大padding。

**布局结构：**

三个核心场景横向排列。

**场景卡片结构：**

```
[顶部图标区：居中，圆角背景]
[场景标题：居中，粗体，白色]
[场景描述：居中，灰色，最多4行]
[适用角色标签：2-3个Tag横向居中]
[底部链接：「了解更多 →」]
```

**三个场景内容：**

**企业研发提效**
图标：代码编辑器图标
描述：研发团队通过 VxonAI 私有化部署，在内网环境下安全调用大模型进行代码生成、代码审查、Bug定位。VxonFort ZT 提供零信任管控，代码不可随意拷贝，Copy操作自动触发审批流，满足合规要求。
角色标签：CTO / 研发负责人 / 安全合规

**运营与数据分析**
图标：图表或数据流图标
描述：运营团队通过 VxonClaw 智能体平台，实时对接业务数据，自动生成分析报告，替代传统 BI 系统。告别繁琐的报表制作，数据驱动决策，一键生成。
角色标签：运营总监 / 数据分析师 / BI负责人

**业务智能化管控**
图标：齿轮或流程图标
描述：销售、财务、客服团队通过 VxonClaw 实现客户管理、商机跟进、财务对账的自动化。替代传统 CRM/OA 系统，一个平台搞定所有业务流程，实时为业务赋能。
角色标签：销售总监 / 财务负责人 / CEO

**场景卡片 hover 效果：**

- 卡片整体上浮
- 顶部图标区域背景变亮，图标光晕扩散
- 底部链接文字向右位移

**入场动画：**

三个卡片从左到右交错入场。

---

### 2.6 合作厂商展示区

**空间布局：**

上下中padding。背景与上下层略有区分。

**布局结构：**

上方一行小字「已对接以下主流大模型厂商」，居中。下方 Logo 墙居中，灰色调。

**Logo 样式：**

每个 Logo 高度适中，横向排列，超出一行则换行。颜色灰色。Hover 时恢复颜色或者微微变亮。

展示的厂商：Google（Gemini）、OpenAI（ChatGPT）、Anthropic（Claude）、DeepSeek、阿里云（Qwen）、智谱AI（GLM）、月之暗面（Kimi）、硅基流动（SiliconFlow）。

---

### 2.7 数据与证明区

**空间布局：**

上下中padding。

**布局：**

横向排列 4-5 个数据指标，每个指标居中。

```
[数字：大字号，白色，强调色光晕]
[描述文字：灰色，说明指标含义]
```

**内容方向（按实际情况填写）：**

- 「500+」— 企业客户信赖
- 「10亿+」— API Token 调用量
- 「7+」— 主流模型覆盖
- 「99.9%」— 服务可用性
- 「12+」— 行业覆盖

**数字动效：**

页面滚动到该区域时，数字从 0 开始滚动到目标值，只触发一次。

---

### 2.8 最终 CTA 区

**空间布局：**

上下大padding，居中内容区。

**布局结构：**

```
[大标题：居中，白色，粗体]
[副标题：居中，灰色]
[两个CTA按钮横向居中]
```

**内容：**

标题：准备好提升你的 AI 能力了吗？
副标题：立即注册，免费试用 100 元额度
按钮：「免费试用」（主）+「预约企业演示」（次）

**背景：**

可以与全站略有区分，比如背景加一层微妙的渐变，或者在内容区背后加一个模糊的光晕效果，透明度极低。

---

### 2.9 Footer

**整体布局：**

分上下两部分。上部四列布局，下部一行版权信息。

**上部 — 四列布局（PC端）：**

**第一列 — 品牌区：**
- Logo（白色）
- 一行公司描述（灰色）
- 社交图标横排（GitHub / Twitter / 微信 / 微信公众号）

**第二列 — 产品：**
标题「产品」
竖排文字链接：VxonAPI / VxonAI / VxonClaw / VxonFort ZT

**第三列 — 公司：**
标题「公司」
竖排文字链接：关于我们 / 加入我们 / 博客 / 联系方式

**第四列 — 支持：**
标题「支持」
竖排文字链接：API文档 / 定价方案 / 服务条款 / 隐私政策

**列链接 hover 效果：**

文字颜色变白，下方出现一条细横线从左向右展开。

**下部 — 版权行：**

一条分隔线，然后一行包含版权信息、ICP备案号、法律链接。

**移动端 Footer：**

四列变为两列，品牌区占满宽度在最上。

---

## 三、VxonAPI 产品页

### 3.1 导航栏

与主站相同结构。Logo 旁的品牌名不变。主菜单中「产品」高亮。

---

### 3.2 Hero 首屏

顶部小字：「VxonAPI — 大模型聚合平台」

大标题：「一个平台，接入所有主流大模型」或「Google、OpenAI、Claude、DeepSeek — 一个接口全部搞定」

副标题：「VxonAPI 已适配 7+ 主流大模型，兼容 OpenAI 标准接口，对标官网 7 折起，量大管饱，技术团队 5 分钟即可完成接入。」

按钮：「免费试用」（主）+「查看定价」（次）

背景动效：在主站的数据流/神经网络动效基础上，加入 API 接口相关的视觉暗示。画面中央有 5-8 条细线从不同方向（上下左右斜向）向中心汇聚，汇聚点是一个 4-6px 的发光核心。这几条线代表不同的模型/数据源汇聚到 VxonAPI 这一个平台。

每条线的透明度在 0.05-0.2 之间随机缓慢变化，各线独立，有光点沿着线向中心缓慢移动，移动速度极慢，象征「数据汇聚」。

---

### 3.3 功能特性区

**布局结构：**

左侧区块标题（大标题+描述，占宽一部分），右侧功能卡片网格（2×3，共6个功能点）。

左侧：功能标题「为什么选择 VxonAPI」，下方一行描述。

右侧六个功能卡片：

1. **多模型聚合** — 图标：层叠的方块 — 一站式接入 Google Gemini、OpenAI GPT、Claude、DeepSeek、Qwen、GLM、Kimi 等，无需逐一对接
2. **价格优势** — 图标：钱包 — 对标官网 7 折起，按量计费，用多少付多少，没有月费套路
3. **极速接入** — 图标：闪电 — 兼容 OpenAI 标准接口格式，现有代码无需改动，5 分钟完成迁移
4. **稳定可靠** — 图标：信号/服务器 — 99.9% 服务可用性保障，高并发承载，7×24 小时稳定运行
5. **灵活切换** — 图标：切换箭头 — 一个 API Key 可自由切换不同模型，实时查询各模型余额
6. **技术文档** — 图标：文档 — 完善的 API 文档，代码示例覆盖 Python/Go/Java/Node.js，开箱即用

每个功能卡片：顶部图标 + 功能标题（粗体）+ 功能描述（灰色，最多3行）。

功能卡片 hover 效果：背景变亮，左边框出现强调色竖线。

---

### 3.4 支持的模型展示区

**布局结构：**

区块标题「支持的模型」，居中，下方模型卡片网格（3列）。

**模型卡片结构：**

每个模型一张卡片，圆角背景，边框。

```
[模型Logo或图标，居中]
[模型名称：粗体，白色，居中]
[模型版本/说明：灰色，居中]
```

展示的模型：OpenAI（GPT-4o / GPT-4 / GPT-3.5）、Anthropic（Claude 3.5 / Claude 3）、Google（Gemini Pro / Gemini Flash）、DeepSeek（DeepSeek V3 / DeepSeek Coder）、阿里云（Qwen Turbo / Qwen Plus / Qwen Max）、智谱AI（GLM-4 / GLM-4V）、月之暗面（Kimi Chat / Kimi Vision）、SiliconFlow（支持更多开源模型）。

卡片 hover 效果：上浮，背景变亮，边框出现强调色光晕。

---

### 3.5 价格方案预览区

**布局结构：**

三个定价卡片横向排列，居中。

**体验版（免费）**
- 价格：免费
- 描述：适合个人体验
- 特性：送 100 元额度 / ChatGPT 3.5 / 1000 次调用/天
- CTA：「立即注册」

**专业版**（推荐，突出显示）
- 价格：对标官网 7 折起
- 描述：适合个人开发者和小团队
- 特性：全模型覆盖 / 无日调用限制 / API 优先响应 / 余额实时查询
- CTA：「免费试用」

**企业版**
- 价格：联系获取报价
- 描述：适合企业团队
- 特性：统一账户管理 / 额度灵活分配 / 专属技术支持 / SLA保障 / 私有化部署可选
- CTA：「联系我们」

中间「专业版」比其他两个略高，背景和边框用强调色，右上角加「推荐」标签。

---

### 3.6 快速开始区（Getting Started）

**布局：**

左右分栏，左侧文字区，右侧代码展示区。

**左侧文字区：**

标题：「5 分钟快速接入」

步骤列表（竖排，每步带序号）：

1. 注册账号，获取 API Key
2. 安装 SDK 或直接调用 REST API
3. 选择模型，开始调用
4. 查看用量，实时管理余额

每个步骤之间间距适中。序号用强调色圆形背景+白色数字。

**右侧代码区：**

一个模拟的终端/代码块，黑底，代码高亮。展示最简单的 API 调用示例（Python 或 cURL）。

右上角有一个小按钮「复制代码」，hover 时出现，点击后文字变成「已复制」。

---

### 3.7 最终 CTA 区

标题：「开始使用 VxonAPI」
副标题：「立即注册，免费获得 100 元额度」
按钮：「免费试用」（主）

---

### 3.8 Footer

与主站 Footer 完全一致。

---

## 四、VxonAI 产品页

### 4.1 导航栏

结构同主站。「产品」下拉中 VxonAI 项高亮。

---

### 4.2 Hero 首屏

顶部小字：「VxonAI — 企业级AI私有化部署」

大标题：「把大模型部署到你的内网，数据不出门」或「企业级AI私有化部署，安全可控，全面提效」

副标题：「VxonAI 支持本地私有化部署大模型，所有数据在内网流转，满足金融、政务等高安全要求。统一账户管理，无缝集成企业现有系统，为办公与研发场景全面提效。」

按钮：「预约企业演示」（主）+「了解定价」（次）

背景动效：加入「安全」「内网」「隔离」相关的视觉暗示。画面中央有一个核心节点（6-8px 发光点），周围有 3 层同心圆环，每层圆环颜色不同（由内到外颜色渐淡），透明度 0.2-0.4。圆环有缓慢的呼吸效果，半径在 ±3% 之间缓慢变化，周期 6-8 秒，三层圆环周期错开，模拟「心跳」。核心节点持续发光，光晕有微妙的脉动。偶尔有一两个数据点从外层「进入」，但在第二层被拦截停顿，然后「消失」被吸收，模拟「API 审计流程」，但这个效果极慢，1-2 分钟才发生一次。

---

### 4.3 核心价值主张区

**布局结构：**

区块标题居中，下方三列价值主张横向排列。

**三个价值主张：**

**数据安全 · 零出站风险**
图标：盾牌或锁
标题：数据不出内网
描述：所有数据流转发生在本地服务器，无外部网络请求，满足等保2.0/GDPR等合规要求。

**统一管控 · 企业级账户体系**
图标：组织架构图或人员图标
标题：集中账户管理
描述：支持 SSO 单点登录，与企业 AD/LDAP 打通。统一账户体系，细粒度权限控制，调用日志全链路审计。

**场景融合 · 深度集成企业系统**
图标：拼图或连接图标
标题：无缝嵌入工作流
描述：与企业 OA、CRM、ERP 等系统深度集成，AI 能力直接赋能业务场景，无需改变现有工作方式。

每个卡片：顶部图标区（居中）+ 标题（居中）+ 描述（居中）。

---

### 4.4 功能特性区

**布局：**

区块标题居左，下方 2×3 功能卡片网格。

**六个功能点：**

1. **本地私有化部署** — 图标：服务器 — 支持主流开源大模型私有化部署，Llama/Qwen/ChatGLM 等，一键启动
2. **统一账户管理** — 图标：用户组 — 企业账户集中管理，部门/团队分级授权，调用量实时统计
3. **内网安全隔离** — 图标：隔离墙 — 网络完全隔离，无外部出口，敏感数据不外流
4. **无缝系统集成** — 图标：API接口 — 提供标准 API 接口，与企业现有系统快速集成
5. **智能编码辅助** — 图标：代码窗口 — 内置代码生成、审查、解释能力，专为研发场景优化
6. **全链路日志审计** — 图标：日志文件 — 所有操作全程留痕，支持导出审计报告

---

### 4.5 部署架构示意区

**布局：**

区块标题，下方一张架构示意图（可以是抽象架构图）。

展示简化的企业内部网络拓扑：
- 左侧：「企业内部网络」+ 员工终端 / OA系统 / CRM系统 / 企业数据库
- 中间：「VxonAI 私有化部署节点」（核心，突出显示，有光晕）
- 整体架构用线条连接，箭头方向表示数据流向（全部在内网闭环）

架构图下方一行文字：「数据不出内网 · 请求全在本地 · 日志全程可查」

---

### 4.6 适用场景区

三个场景卡片横向排列。

**场景1：企业研发团队**
图标：代码图标
描述：研发人员通过 VxonAI 私有化环境进行代码生成、代码审查、Bug 定位，代码和对话数据完全不离开企业内网，安全合规。

**场景2：金融/政务行业**
图标：银行/政务图标
描述：金融、政务等高敏感行业，数据管控要求严格，VxonAI 私有化部署满足等保合规要求，支持信创环境。

**场景3：集团型企业**
图标：组织架构图标
描述：集团型企业多部门、多子公司统一账户管理，额度灵活分配，总部统一管控，子公司独立使用。

---

### 4.7 定价/咨询区

居中内容区，不展示具体价格（私有化部署需要根据企业规模和需求定制）。

标题：「企业级定价，灵活定制」
副标题：「私有化部署方案根据企业规模、模型选择、部署环境定制」
按钮：「预约商务咨询」（主）+「下载解决方案白皮书」（次）

---

### 4.8 最终 CTA 区

标题：「让 AI 安全可控地服务你的企业」
副标题：「预约企业演示，了解私有化部署方案」
按钮：「预约演示」（主）

---

### 4.9 Footer

与主站一致。

---

## 五、VxonClaw 产品页

### 5.1 导航栏

结构同主站。「产品」下拉中 VxonClaw 项高亮。

---

### 5.2 Hero 首屏

顶部小字：「VxonClaw — 企业智能体平台」

大标题：「把 AI 嵌入业务流程，而不是做个聊天机器人」或「企业智能体平台，替代 BI、CRM、OA，全面赋能业务」

副标题：「VxonClaw 深度融合运营、财务、销售、经管等业务部门，替代传统 BI 系统、CRM 系统、OA 系统，一个平台实时为业务赋能，帮助企业降本增效。」

按钮：「免费试用」（主）+「预约演示」（次）

背景动效：加入「业务流」「流程」「连接」相关的视觉。画面中有 5-6 个节点散布，节点之间有连线，但不是全连接，而是形成一种「星形+链形」的混合结构，象征不同业务部门连接到统一平台。每个节点是一个小光点，持续缓慢闪烁（透明度 0.4-1，周期 2-5 秒随机）。连线上偶尔有光点从一端流到另一端（数据在部门之间流动），但数量极少，速度极慢，大部分时候连线是静态的半透明细线。

---

### 5.3 核心问题区（Before / After 对比）

**布局结构：**

区块标题居中，下方左右对比布局。

**左列 — 传统方式（问题列表）：**

标题：「传统企业软件的问题」

问题1：系统孤岛 — BI一套、CRM一套、OA一套，数据不通，操作繁琐
问题2：报表滞后 — 数据分析靠人工，出报表要等几天，业务决策慢半拍
问题3：实施复杂 — 传统系统上线周期长，定制开发成本高，维护负担重
问题4：成本高昂 — 多个系统叠加采购，年费维保费压得喘不过气

每个问题用灰色调，前面加一个 ✗ 符号。

**右列 — VxonClaw（解决方案）：**

标题：「VxonClaw 如何解决」

方案1：一个平台 — 运营、财务、销售、经管全覆盖，数据互通，操作统一
方案2：实时分析 — 业务数据自动采集，AI 实时分析，报表一键生成
方案3：快速上线 — SaaS 模式，5 分钟开通，即开即用，无需定制
方案4：降本增效 — 一个平台替代多个系统，大幅降低采购和维护成本

每个方案用强调色点缀，前面加 ✓ 符号。

**对比入场动画：**

左列从左滑入，右列从右滑入，时序相同。

---

### 5.4 功能模块区

**布局结构：**

区块标题居中，下方功能模块网格（2×3，共6个模块）。

**六个功能模块：**

**智能BI分析**
图标：图表/数据图标
描述：自动对接业务数据库，AI 实时分析，告别传统 BI 的复杂报表制作，一键生成数据洞察报告，数据驱动决策。

**CRM 融合**
图标：客户关系图标
描述：智能管理客户全生命周期，从线索到成单到复购全流程自动化，AI 辅助商机判断和跟进提醒。

**OA 办公集成**
图标：审批流程图标
描述：与现有 OA 系统深度集成，AI 辅助审批、报销、考勤等日常办公流程，提效减负。

**财务管控**
图标：财务报表图标
描述：自动对接财务数据，实时监控收支、预算执行情况，AI 自动生成财务分析，异常预警。

**运营自动化**
图标：齿轮自动化图标
描述：运营日常重复性工作自动化，如数据采集、报表生成、群发通知，释放运营团队精力。

**销售赋能**
图标：增长图标
描述：AI 辅助销售话术生成、客户画像分析、商机评分，销售团队效率翻倍。

模块卡片结构：顶部图标区（居左）+ 模块标题（粗体）+ 模块描述（灰色）+ 底部功能Tag。

卡片 hover 效果：整体上浮，左边框出现强调色竖线，背景变亮。

入场动画：六个模块以「Z」字形顺序入场。

---

### 5.5 部门覆盖区

**布局：**

区块标题，下方公司组织架构图形式的抽象展示。

中央一个大的文字「VxonClaw」，周围六个方向放射出线条，连接到六个部门图标：运营部 / 财务部 / 销售部 / 技术部 / 行政部 / 管理部。

每个部门图标下方一行小字说明这个部门在 VxonClaw 里主要用哪个功能模块。

整体做成一个抽象的放射状图形，用线条连接。线条本身有持续的发光脉冲：每条线条的透明度在 0.1-0.4 之间缓慢变化，周期 3-6 秒，各线条独立变化，模拟「平台在持续连接所有部门」。中央 VxonClaw 文字/Logo 区域有持续的光晕呼吸，周期 4-5 秒。

---

### 5.6 集成能力展示区

**布局：**

区块标题居中，下方集成的系统Logo墙（横排，灰色调）。

集成的系统类型：ERP系统 / CRM系统 / OA系统 / 财务系统 / HR系统 / 数据库 / BI工具 / 客服系统

---

### 5.7 定价预览区

三个套餐横向排列。

**体验版（免费）**
适合小团队体验，基础功能模块，5个用户，5GB数据存储。

**专业版**（推荐，突出显示）
适合成长型企业，全功能模块，无限用户，按量计费，统一账户管理。

**企业版**
适合大规模企业，全功能+定制开发，专属客户成功经理，SLA保障。

---

### 5.8 最终 CTA 区

标题：「让 AI 真正嵌入你的业务流程」
副标题：「立即开始，免费试用基础版」
按钮：「免费试用」（主）+「预约演示」（次）

---

### 5.9 Footer

与主站一致。

---

## 六、VxonFort ZT 产品页

### 6.1 导航栏

结构同主站。「产品」下拉中 VxonFort ZT 项高亮。

---

### 6.2 Hero 首屏

顶部小字：「VxonFort ZT — 零信任AI编码安全堡垒机」（加上「即将上线」标签）

大标题：「Claude Code 进不来出不去」或「零信任AI编码安全堡垒机，代码不外泄，合规有保障」

副标题：「VxonFort ZT 将 Claude Code 封装进零信任管控体系，网络隔离、研发区隔离、出站API审计过滤，Copy操作自动触发审批流，审批通过方可释放，满足企业等保合规要求。」

按钮：「预约企业演示」（主，仅限按钮可用）

背景动效：加入「安全」「锁」「边界」「审计」相关的视觉。画面中央是一个被严格「包裹」的核心：最内核是一个 6-8px 的白色发光核心；第二层是一个不规则多边形（类似多边形的盾牌轮廓），边框颜色强调色，透明度 0.4，边框有持续的微弱闪烁；第三层是一个更大的圆形轮廓，透明度 0.2，缓慢旋转（速度极慢，60-90 秒完成一圈）；最外层有 4-6 条细线从外向内延伸，但每条线在距离核心 30% 的位置就被「拦截」，线端点有一个小圆点，表示「流量被审计」。所有层都在缓慢呼吸（透明度 ±10%），但呼吸不同步，层次感强。核心有稳定的强光（外发光）。

---

### 6.3 即将上线预告区

居中内容区，强调「即将上线」。

```
[「🔒 敬请期待」标签，居中]
[产品名称大字，居中，白色，粗体]
[发布时间预估（如有），居中，灰色]
[CTA按钮组：两个按钮横向居中]
```

CTA 按钮：
- 「预约企业演示」（主按钮，强调色，可点击）
- 「填写需求表单」（次按钮，填写内测资格或采购意向）

下方可选：留下联系方式，产品上线后第一时间通知。

---

### 6.4 核心能力预告区

**布局结构：**

区块标题居中，下方四大能力纵向排列（竖排卡片）。

**四大能力：**

**零信任架构 · 最小权限原则**
图标：盾牌/锁
标题：零信任安全架构
描述：任何访问请求均需验证，任何操作均需授权。Claude Code 只能在受控环境内运行，无法直接访问外部网络，代码不可直接外传。

**网络隔离 · 研发区完全封闭**
图标：隔离墙
标题：网络完全隔离
描述：研发区域与办公网络物理隔离，无外部出口。AI 编码工具的所有请求只能在内部流转，数据不出研发区。

**API 审计 · 出站流量全记录**
图标：日志/审计图标
标题：出站API审计过滤
描述：所有向外部 AI 服务的 API 请求均经过审计层，可设置白名单/黑名单规则，敏感数据自动过滤或脱敏。

**Copy审批流 · 代码外传需审批**
图标：审批流程图标
标题：Copy 操作审批流
描述：任何代码复制（Copy/Paste）操作自动触发审批流程，提交后需指定审批人审核通过，代码方可释放。全程操作留痕，支持导出审计报告。

每个能力卡片：左侧图标区 + 右侧文字区（标题+描述）。卡片之间间距适中，用竖线分隔。

---

### 6.5 与传统方案对比区

**布局：**

左右对比，聚焦在「编码安全」这个场景。

**左列 — 传统 Claude Code 使用方式（风险）：**

- 代码直接拷贝，存在数据泄露风险
- 无操作审计，出现问题无法追溯
- 无访问控制，任何人可随意使用
- 无网络隔离，AI 请求直接出站

**右列 — VxonFort ZT（管控）：**

- Copy 操作需审批，代码方可外传
- 所有操作全程留痕，可导出审计报告
- 细粒度权限控制，角色分级授权
- 网络完全隔离，AI 请求内网闭环

---

### 6.6 适用行业/场景区

四个场景卡片横向排列。

**金融行业**
描述：金融行业代码安全要求极高，VxonFort ZT 满足金融监管合规要求，代码不出内网，操作全程审计。

**政务单位**
描述：政务单位等保合规要求严格，VxonFort ZT 支持信创环境，满足等保2.0三级要求，一键部署启动。

**大型科技公司**
描述：科技公司研发资产是核心竞争力，VxonFort ZT 为研发区提供零信任安全管控，保护代码资产不外泄。

**研发外包场景**
描述：研发外包场景下，外部人员使用 AI 编码工具的风险完全可控，所有代码操作可审计，外传需审批。

---

### 6.7 预约/咨询区

居中内容区，展示为「预约演示 / 填写需求」。

```
[标题：「产品即将上线，提前预约抢先体验」]
[副标题：「留下联系方式，产品上线后第一时间通知您」]
[输入框：姓名 / 公司名称 / 邮箱 / 手机 / 需求描述]
[提交按钮：「提交预约」]
```

表单设计简洁，4-5个字段。

---

### 6.8 Footer

与主站一致。

---

## 七、全站通用规范

### 页面入场流程

任何页面加载时的标准流程：

1. 页面骨架先出现（快速）
2. 导航栏先稳定（固定定位）
3. Hero 区域文字从上到下交错出现（每个元素延迟 100ms）
4. 下方模块依次进入视口时触发入场动画
5. 全程控制在 2s 内完成所有可见元素的动画

### 滚动交互

- 平滑滚动
- 导航栏滚动一定距离后触发背景模糊效果
- 锚点跳转时平滑滚动
- 页面右侧可以放一个极简的进度条或锚点导航（可选）

### Hover 状态汇总

| 元素 | Hover效果 |
|------|----------|
| 主按钮 | 上浮，外发光扩散 |
| 次按钮 | 背景变亮，边框变强调色 |
| 菜单文字 | 白色，底部横线展开 |
| 下拉菜单项 | 背景变亮，左边强调色竖线 |
| 产品卡片 | 上浮，边框变亮 |
| 功能卡片 | 背景变亮，左边框强调色竖线 |
| CTA链接 | 文字向右位移 |
| Logo（页脚） | 恢复原始颜色 |
| Tab切换项 | 下划线滑动到当前项 |

### 页面加载状态

- API 数据加载时展示骨架屏（Skeleton Screen），与实际布局一致
- 加载失败时展示简洁的 error 提示 + 重试按钮
- 表单提交时按钮显示 loading 状态，不可重复点击

### 移动端适配

- 断点：768px（Tablets）/ 480px（Mobile）
- 移动端 Hero 标题字号缩小，副标题缩小
- 产品卡片从横排变竖排
- 功能网格从 2×3 变 1 列
- 下拉 Mega Menu 变成全屏展开面板
- Footer 四列变两列
- 所有间距整体缩小 30-40%

---

## 八、各页面汇总清单

| 页面 | URL | 核心模块 |
|------|-----|---------|
| 主站 | / | Nav + Hero + 产品家族 + 优势 + 场景 + 厂商 + 数据 + CTA + Footer |
| VxonAPI | /product/api | Nav + Hero + 功能特性 + 模型展示 + 定价预览 + 快速开始 + CTA + Footer |
| VxonAI | /product/ai | Nav + Hero + 价值主张 + 功能特性 + 部署架构 + 适用场景 + 定价咨询 + CTA + Footer |
| VxonClaw | /product/claw | Nav + Hero + 问题对比 + 功能模块 + 部门覆盖 + 系统集成 + 案例 + 定价 + CTA + Footer |
| VxonFort ZT | /product/fort | Nav + Hero + 即将上线 + 能力预告 + 场景对比 + 预约咨询 + Footer |

---

## 九、我们不希望看到的设计

- 五彩斑斓的渐变
- 到处堆放「全球领先」「遥遥领先」这种空洞口号
- 密密麻麻的文字块
- 过度的 3D 效果或者闪来闪去的动效
- 传统企业官网那种信息堆砌的压迫感

---

## 十、开放给你发挥的部分

以上是我们的一些基本原则和内容框架，具体的排版布局、配色方案、动效细节，可以充分发挥创意，只要核心内容传达清晰就行。

希望出来的网站：**看了就想点进去了解，专业有质感，区别于市面上的套版网站，让用户觉得这家公司是认真做事的。**
